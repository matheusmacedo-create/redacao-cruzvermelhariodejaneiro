'use client'
import { useRef,useState } from 'react'
import { useRouter } from 'next/navigation'
import { Camera,Trash2 } from 'lucide-react'
import { Avatar } from '@/components/ui/avatar'
import { Button } from '@/components/ui/button'
export function AvatarUpload({initials,color,path,name}:{initials:string;color?:string;path?:string|null;name:string}){const input=useRef<HTMLInputElement>(null),router=useRouter();const [busy,setBusy]=useState(false),[error,setError]=useState('');const src=path?`/api/private-blob?pathname=${encodeURIComponent(path)}`:null;async function upload(){const file=input.current?.files?.[0];if(!file)return;setBusy(true);setError('');const body=new FormData();body.set('file',file);const res=await fetch('/api/profile/avatar',{method:'POST',body});const json=await res.json();setBusy(false);if(!res.ok){setError(json.error);return}router.refresh()}async function remove(){setBusy(true);const res=await fetch('/api/profile/avatar',{method:'DELETE'});setBusy(false);if(res.ok)router.refresh();else setError((await res.json()).error)}return <div className="flex flex-col gap-3"><div className="flex items-center gap-4"><Avatar initials={initials} color={color} src={src} alt={`Foto de ${name}`} size="xl"/><div className="flex flex-wrap gap-2"><input ref={input} type="file" accept="image/jpeg,image/png,image/webp" className="sr-only" onChange={upload}/><Button type="button" variant="outline" onClick={()=>input.current?.click()} disabled={busy}><Camera className="size-4"/>{busy?'Enviando…':'Trocar foto'}</Button>{path&&<Button type="button" variant="ghost" onClick={remove} disabled={busy}><Trash2 className="size-4"/>Remover</Button>}</div></div><p className="text-xs text-muted-foreground">JPEG, PNG ou WebP, até 2 MB.</p>{error&&<p className="text-sm text-destructive">{error}</p>}</div>}
