# ─── Supabase — projeto RedacaoCruzVermelha-Rj (ref: wlbbfkudeibalkaqphpo) ───
# Copie este arquivo para .env.local e preencha a service role key.
#   cp .env.example .env.local

# Client (browser + server components). Valores públicos: vão para o bundle.
NEXT_PUBLIC_SUPABASE_URL=https://wlbbfkudeibalkaqphpo.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_LA0yYrCep1il3dHTsumHuQ_Q4WVftBm

# Fallback legado, usado por lib/supabase/{client,server}.ts e proxy.ts
# quando PUBLISHABLE_KEY não está definida. Opcional.
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Admin (lib/supabase/admin.ts — server-only, ignora RLS).
# SEGREDO: nunca commitar, nunca prefixar com NEXT_PUBLIC_.
# Pegue em: Dashboard → Project Settings → API Keys → service_role
SUPABASE_URL=https://wlbbfkudeibalkaqphpo.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# ─── Publicação no site institucional (FTP da Hostinger) ───
# Grava os artigos publicados em cruzvermelhariodejaneiro.org/noticias/.
# SEGREDO: FTP_PASSWORD nunca pode ganhar o prefixo NEXT_PUBLIC_ — isso
# entregaria a senha de escrita do site a qualquer visitante.
#
# Sobre o escopo da conta, há uma escolha real a fazer:
#
#  - Conta LIMITADA a /public_html/noticias: o pior caso possível é quebrar a
#    seção de notícias. Mais segura para o dia a dia de publicar matérias.
#  - Conta com acesso à raiz do site: necessária para o botão "Ligar o
#    formulário" da Central de e-mail, que reescreve a seção da newsletter no
#    index.html da home.
#
# Com a conta ampla, o que protege a home no fluxo de matérias continua sendo
# caminhoSeguro(), que recusa qualquer caminho que tente sair de FTP_BASE_DIR.
# Escrever fora dele só acontece por uma porta separada, que aceita um único
# nome de arquivo literal (index.html) e só depois de o conteúdo baixado provar
# que aquilo é mesmo a home.
FTP_HOST=45.152.46.153
# O usuário é o que o painel mostrar depois de criar a conta — costuma vir com
# o prefixo da hospedagem, no formato u448697994.<nome>.
FTP_USER=
FTP_PASSWORD=
# Pasta de trabalho dentro do que a conta de FTP enxerga.
#
# Com a conta limitada a .../public_html/noticias no painel da Hostinger, ela já
# entra confinada e o valor certo é "/" — apontar para /noticias aqui criaria
# noticias/noticias. Só use /noticias se a conta abrir na raiz do site.
#
# Na dúvida, /api/admin/ftp-check mostra a listagem da pasta em que entrou:
# se aparecer index.html, a conta está na raiz e este valor precisa ser
# /noticias. De todo jeito, caminho que tente sair desta pasta é recusado.
FTP_BASE_DIR=/

# Endereço público correspondente à pasta acima. O diagnóstico grava um arquivo
# por FTP e busca este endereço para provar que a pasta é mesmo publicada — é
# daqui que sai o link do artigo e a URL da imagem que o Instagram baixa.
SITE_PUBLIC_BASE_URL=https://cruzvermelhariodejaneiro.org/noticias

# Afrouxa a verificação do certificado TLS. Necessário enquanto FTP_HOST for um
# endereço IP, porque certificado não casa com IP. A sessão segue cifrada, mas
# deixa de provar a identidade do servidor. Ative o hostname na Hostinger e
# remova esta variável quando puder.
# FTP_TLS_INSECURE=1

# --- Publicação nas redes sociais (Upload-Post) ---------------------------
# Publica no Facebook, Instagram, LinkedIn, X e Threads sem precisar de um app
# próprio aprovado pela Meta: o OAuth roda contra o app já aprovado do
# Upload-Post. Gere a chave em https://app.upload-post.com/api-keys.
# Nunca prefixe com NEXT_PUBLIC_ — a chave publica em nome da instituição e
# variáveis com esse prefixo vão para o navegador de todo visitante.
UPLOAD_POST_API_KEY=

# Nome do perfil no Upload-Post onde ficam as contas conectadas. Precisa ser
# EXATAMENTE o nome que aparece em app.upload-post.com/manage-users — um nome
# que não existe faria a Redação publicar num perfil vazio, sem erro visível.
# /api/admin/redes-check lista os nomes exatos da conta.
UPLOAD_POST_PROFILE=

# Id da página do Facebook de destino. Descubra em /api/admin/redes-check
# depois de conectar a conta.
UPLOAD_POST_FACEBOOK_PAGE_ID=

# --- Módulo de IA (OpenAI) ------------------------------------------------
# Gera imagens no enquadramento de cada rede e propõe a legenda dentro do
# limite do canal. Sem esta variável o módulo simplesmente não aparece na
# tela — botão que só produz "falta a chave" é pior do que botão nenhum.
#
# Crie uma chave DEDICADA a este uso em platform.openai.com/api-keys e ponha
# um limite de gasto próprio nela (Settings → Limits). Assim um erro de código
# não vira fatura.
#
# NUNCA prefixe com NEXT_PUBLIC_: variáveis com esse prefixo vão para o
# navegador de qualquer visitante, e esta chave é cobrada por uso.
OPENAI_API_KEY=

# Nomes dos modelos. Ficam em variável porque a OpenAI renomeia e aposenta
# modelos com frequência — trocar um nome não pode exigir deploy. Em branco,
# usa os padrões do código. /api/admin/ia-check lista o que a sua conta
# enxerga e diz se o nome configurado existe.
# OPENAI_IMAGE_MODEL=gpt-image-2
# OPENAI_TEXT_MODEL=gpt-5-mini

# Teto de imagens geradas por mês, por espaço. Imagem custa a cada clique;
# sem teto, um engano em laço vira conta. Em branco, 60.
# OPENAI_IMAGE_LIMITE_MENSAL=60

# Quanto raciocínio pedir ao modelo de texto. A família GPT-5 raciocina por
# padrão, e adaptar legenda é reescrita curta com instrução clara — o caso em
# que "minimal" existe. A primeira chamada real, sem isto, levou 13,6s.
#
# Os valores aceitos dependem do modelo (minimal, low, medium, high…). Se a
# OpenAI recusar o que estiver aqui, a chamada se refaz sem o parâmetro em vez
# de quebrar, e o aviso vai para o log. Em branco, desliga o envio.
# /api/admin/ia-check?testar=1 mostra os segundos e os tokens de raciocínio de
# cada chamada — é por ali que se confere se o ajuste está valendo.
# OPENAI_REASONING_EFFORT=minimal

# O modelo que ESCREVE a matéria no "Melhorar com IA" — separado do de
# legenda de propósito: legenda cabe ao mini; matéria completa no mini sai
# rasa. Em branco, gpt-5 com esforço medium.
# OPENAI_WRITER_MODEL=gpt-5
# OPENAI_WRITER_EFFORT=medium

# ─── Anthropic (melhorar o texto da matéria com o Claude) ──────────────────
#
# Crie a chave em console.anthropic.com (API Keys) e cole SÓ aqui/na Vercel —
# chave que aparece em chat ou print está comprometida e precisa ser trocada.
# NUNCA prefixe com NEXT_PUBLIC_. O código também aceita o nome CLAUDE_API,
# mas o padrão é este:
ANTHROPIC_API_KEY=

# Modelo de texto do Claude. Em branco, usa o padrão do código
# (claude-opus-5). /api/admin/ia-check confere se a chave chegou.
# ANTHROPIC_TEXT_MODEL=claude-opus-5

# ─── Cérebro (cerebrocruzvermelha) ─────────────────────────────────────────
# O Cérebro observa as contas oficiais do Rio e entrega pautas prontas de
# triagem em /api/pauta. A Redação as mostra na tela Cérebro e no painel de
# Publicações, importa para o hub e devolve as recusas com motivo.
#
# Em branco, usa https://cerebrocruzvermelha.vercel.app. Sem alcance ao
# Cérebro, a tela avisa e o resto da Redação não é afetado.
# CEREBRO_URL=

# Se o Cérebro fechar o contrato com PAUTA_TOKEN, o mesmo valor entra aqui e
# viaja como Bearer em toda leitura e recusa.
# CEREBRO_TOKEN=

# Fecha a rota /api/cerebro/contexto — o que esta Redação devolve ao Cérebro
# (títulos publicados e atividades do Registrar, fase 2 do contrato). O mesmo
# valor vai no REDACAO_CONTEXTO_TOKEN do Cérebro. Em branco, a rota fica
# aberta como o contrato de pauta: só saem títulos, nunca corpo ou contato.
# CEREBRO_CONTEXTO_TOKEN=

# ─── Newsletter (Resend) ───────────────────────────────────────────────────
# Entrega os e-mails da newsletter e o convite de confirmação da inscrição.
# Crie a chave em https://resend.com/api-keys.
#
# SEGREDO: nunca com o prefixo NEXT_PUBLIC_. Quem tem esta chave envia e-mail
# em nome da Cruz Vermelha, e remetente humanitário sequestrado é um estrago
# que não se desfaz com um deploy.
#
# Sem esta variável a inscrição CONTINUA funcionando: o endereço é guardado
# como pendente e a pessoa é avisada de que o convite sai quando o envio
# entrar no ar. Nada se perde.
RESEND_API_KEY=

# Remetente das mensagens. Precisa ser de um domínio VERIFICADO no Resend —
# domínio não verificado devolve 403 em toda chamada, e é o erro nº 1 de quem
# começa.
#
# ATENÇÃO AO SUBDOMÍNIO: para o Resend, "cruzvermelhariodejaneiro.org" e
# "noticias.cruzvermelhariodejaneiro.org" são domínios DIFERENTES — cada um com
# sua chave DKIM e sua verificação. Verificar um não verifica o outro.
#
# Em branco, "Cruz Vermelha RJ <noticias@noticias.cruzvermelhariodejaneiro.org>",
# que é o subdomínio verificado na conta.
# NEWSLETTER_REMETENTE=

# Para onde vão as respostas de quem responder a newsletter. Sem isto, ninguém
# lê o que a pessoa escreveu de volta.
# NEWSLETTER_RESPONDER_PARA=comunicacao@cruzvermelhariodejaneiro.org

# Base dos links de confirmação e de saída dentro das mensagens.
#
# Não use VERCEL_URL: aquilo é o endereço do deploy, que muda a cada
# publicação, e link de saída quebrado vira denúncia de spam. Em branco,
# https://redacao.cruzvermelhariodejaneiro.org.
# NEWSLETTER_URL_BASE=

# Qual espaço de trabalho recebe as inscrições. Em branco, o único que existir.
# Só é necessária se um dia houver mais de um — aí a rota falha pedindo isto,
# em vez de escolher um na sorte.
# NEWSLETTER_WORKSPACE_ID=

# Origens extras autorizadas a enviar o formulário, separadas por vírgula.
# O site institucional (com e sem www) já vem liberado no código.
# NEWSLETTER_ORIGENS=
